spring:
  application:
    name: spring-7-rest-mvc

  profiles:
    active: localmysql

  data:
    web:
      pageable:
        default-page-size: 10
        one-indexed-parameters: false
        max-page-size: 100
      sort:
        default-sort: sort

logging:
  level:
    com:
    springframework: DEBUG